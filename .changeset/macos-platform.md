---
'touchpress': minor
---

Add `macos` as a platform. touchpress drives an app on the host Mac through agent-device, and locators skip the close, full-screen, and minimize buttons that XCUI adds to every macOS window.
