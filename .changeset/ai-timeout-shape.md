---
'touchpress': patch
---

The `ai-timeout` error info is back to its 0.2 shape, `instruction`, `timeoutMs`, and `screen`. The `command` and `asked` fields added in 0.3.0 said nothing `instruction` did not, and the deprecated alias that carried `instruction` alongside them is gone with them.
