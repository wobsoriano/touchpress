---
'touchpress': minor
---

Add `use.target`, one object whose `provider` is `'local'`, `'browserstack'`, `'aws-device-farm'`, or `'limrun'`, to run a project on a local simulator or emulator or on a hosted device
